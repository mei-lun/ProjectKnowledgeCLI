<!-- 本文件由 project-kb 自动生成，请勿手动编辑。 -->

# 模块：root

## 文件

- `.project-kb.yml`（配置）
- `pyproject.toml`（配置）

## 符号

- `pyproject.toml::requires`：config，位于 `pyproject.toml:2`（可信度 0.90）
- `pyproject.toml::build-backend`：config，位于 `pyproject.toml:3`（可信度 0.90）
- `pyproject.toml::name`：config，位于 `pyproject.toml:6`（可信度 0.90）
- `pyproject.toml::dynamic`：config，位于 `pyproject.toml:7`（可信度 0.90）
- `pyproject.toml::description`：config，位于 `pyproject.toml:8`（可信度 0.90）
- `pyproject.toml::readme`：config，位于 `pyproject.toml:9`（可信度 0.90）
- `pyproject.toml::requires-python`：config，位于 `pyproject.toml:10`（可信度 0.90）
- `pyproject.toml::license`：config，位于 `pyproject.toml:11`（可信度 0.90）
- `pyproject.toml::authors`：config，位于 `pyproject.toml:12`（可信度 0.90）
- `pyproject.toml::keywords`：config，位于 `pyproject.toml:13`（可信度 0.90）
- `pyproject.toml::classifiers`：config，位于 `pyproject.toml:14`（可信度 0.90）
- `pyproject.toml::dependencies`：config，位于 `pyproject.toml:22`（可信度 0.90）
- `pyproject.toml::project-kb`：config，位于 `pyproject.toml:25`（可信度 0.90）
- `pyproject.toml::package-dir`：config，位于 `pyproject.toml:28`（可信度 0.90）
- `pyproject.toml::version`：config，位于 `pyproject.toml:31`（可信度 0.90）
- `pyproject.toml::where`：config，位于 `pyproject.toml:34`（可信度 0.90）
- `pyproject.toml::testpaths`：config，位于 `pyproject.toml:37`（可信度 0.90）
- `pyproject.toml::addopts`：config，位于 `pyproject.toml:38`（可信度 0.90）

## 结构关系

- 未检测到结构关系
