# Lua/Skynet 真实业务问题候选

目标样本：`C:\Users\mei\Desktop\11.0.0.0`  
状态：等待 D-007 指定业务标准答案负责人  
写入策略：未经 D-003 授权，不在目标项目原目录初始化或写入任何文件。

以下问题来自审计要求和 Lua/Skynet 项目常见开发任务，只是候选问题，不代表答案已经确认：

1. 玩家登录从哪个启动入口、协议和业务方法进入？
2. 背包使用物品依次经过哪些协议、服务、领域模块和持久化操作？
3. 玩家数据在哪些时机加载、缓存、写回和离线持久化？
4. 新增一种礼包应该复用哪个现有实现，扩展点和配置分别在哪里？
5. 新增一个 Skynet 服务需要新增哪些脚本、启动配置、服务名和依赖？
6. 客户端协议如何从 protocol/run/exec 派发到具体业务方法？
7. 修改资源配置时必须保持哪些 ID、引用、兼容和热更新不变量？
8. 修改一个服务会影响哪些调用方、测试、配置和知识文档？
9. 公会创建流程涉及哪些服务、数据库表和跨服调用？
10. 邮件附件领取如何保证幂等并避免重复发放？
11. 充值到账、礼包发放和失败补偿分别由哪些模块负责？
12. 每日、每周重置任务由哪个调度器触发，时间边界如何处理？
13. cluster proxy/call/send 的服务地址如何建立和失效重连？
14. 新增或修改数据库字段需要同步哪些 SQL、加载器和兼容逻辑？
15. `.conf` 配置如何覆盖默认值并传递给启动服务？
16. 五个 main 入口分别启动哪些进程、服务和业务域？
17. 热更新一个 Lua 模块时哪些状态可以保留，哪些必须重建？
18. RPC 超时、重试和重复请求如何保持业务幂等？
19. 新增 GM 命令需要经过哪些权限、解析、派发和审计环节？
20. 新增一种协议消息需要修改协议定义、生成代码、派发表和业务处理的哪些位置？

每条问题冻结前必须补齐：标准答案摘要、文件锚点、稳定符号、调用路径、推荐扩展点、业务不变量、验证命令、负责人和确认日期。
